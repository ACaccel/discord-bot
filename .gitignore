/node_modules
/dist
/logs
/errors
/data
*.log

# Personal Claude Code settings (machine-specific allowlists, paths).
# `.claude/agents/` IS tracked — those are shared dev tooling.
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

.env
config.json
guild_profile.json
avatar_url_list.txt
remove_list.txt
snippet.ts
debug.sh
/coverage

# Ops tools — operator-supplied configs and per-run log output.
# `config.json` is already matched by the global rule above; the
# explicit entries below keep the tool-specific layout self-documenting.
tools/msg_backup/config.json
tools/msg_backup/logs/
tools/msg_backup/tmp/
tools/verify_db/config.json

