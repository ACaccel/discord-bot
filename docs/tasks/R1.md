# R1 任務 — 拆解 BaseBot

> 詳細設計：[docs/design/R1.md](../design/R1.md)　|　需求：[proposal §4](../proposal.md#4-r1--拆解-basebot)　|　高階設計：[HLD §4](../high-level-design.md#4-r1--拆解-basebot)

## 先決條件（必須先做）

- [x] 補齊 BaseBot end-to-end contract 測試
  - [x] `test/integration/bot/run-orchestration.int.test.ts`：覆蓋 `BaseBot.run()` 完整啟動序列
  - [x] `test/integration/bot/event-bridge.int.test.ts`：覆蓋現有 8 條 `client.on(...)` 的關鍵分支
  - [x] 在 main commit 前，所有新測試對**未拆解的** BaseBot 跑綠（作為 baseline）

## 子任務

### 1. 新增 `GuildRegistrar`

- [x] 新檔 `src/bot/guild-registrar.ts`：照 design R1 §3.2 的 TS 骨架建立 class
- [x] 從 `BaseBot.registerGuild()` 與 reaction-fetch 輔助搬入實作
- [x] 新檔 `test/unit/bot/guild-registrar.test.ts`：照 design R1 §6.1 覆蓋 happy / boundary / error case
- [x] BaseBot 改為持有 `private readonly registrar: GuildRegistrar`，刪除原 `registerGuild()` 方法

### 2. 新增 `ClientEventBridge`

- [x] 新檔 `src/bot/client-event-bridge.ts`：照 design R1 §3.2 建立 class
- [x] 從 `BaseBot.listen()` 搬入 8 條 `client.on(...)` 註冊
- [x] 新增 `ReactionHandlerPort` / `ClientEventBridgeConfig` 型別（design R1 §3.2）
- [x] 新檔 `test/unit/bot/client-event-bridge.test.ts`
- [x] BaseBot 改為持有 `private readonly eventBridge: ClientEventBridge`，刪除原 `listen()` 方法

### 3. 新增 `GuildDbConnector`

- [x] 新檔 `src/bot/guild-db-connector.ts`：照 design R1 §3.2 建立 class
- [x] 從 `BaseBot.connectGuildDB()` / `connectOneGuild()` 搬入實作
- [x] 新檔 `test/unit/bot/guild-db-connector.test.ts`
- [x] BaseBot 改為持有 `private readonly dbConnector: GuildDbConnector`，刪除原 connect 方法

### 4. 重組 `BaseBot`

- [x] 重寫 `src/bot/index.ts`：照 design R1 §3.2 的 BaseBot 骨架（thin lifecycle owner）
  - [x] 保留：`constructor`、`use(plugin)`、`run()`、`shutdown()`、IoC container 存取器（getter）
  - [x] `run()` 內部依序：`setupContainer → setupTranslator → dbConnector.connectAll → registrar.register × N → eventBridge.attach → host.startAll`
  - [x] 刪除：直接觸碰 Discord raw event 的程式碼、直接做 channel/role 解析的程式碼、直接開 Mongo 連線的程式碼
- [x] 順手套用 R6.4 命名一致化（Handler Map 一律複數）
- [x] 順手套用 R6.5：所有 import 移到檔頭，`sharedConnectionManagers` 與 helper 搬到 import 區之後
- [x] BaseBot lifecycle 順序契約（`use → run → shutdown`）保持不變

### 5. Subclass 同步

- [x] `src/bot/nijika/nijika.ts`：對齊新的 BaseBot 欄位 / getter 名稱
- [x] `src/bot/konata/konata.ts`：同上
- [x] `src/bot/tomori/tomori.ts`：同上
- [x] `src/bot/msg-archive/msg-archive.ts`：同上

### 6. Handler / 下游同步

- [x] grep `bot.buttonHandler` / `bot.modalHandler` / `bot.ssmHandler` / `bot.reactionHandler` / `bot.help_msg` / `bot.guild_num` / `bot.debug_ch` 等舊命名的所有引用點並改名
- [x] grep 所有 `bot.guildInfo[...]` / `bot.client.on(...)` 直接使用點，確認沒有跨層繞過 collaborator 的呼叫

### 7. 整合測試

- [x] 既有 `test/integration/interaction-router/router-dispatch.int.test.ts` 全綠
- [x] 先決條件補入的 `run-orchestration` / `event-bridge` 整合測試對拆解後 BaseBot 仍綠

### 8. 文件 / 審閱

- [x] 跑 `architecture-reviewer` agent Audit BaseBot 拆解結果，verdict = PASS
      （此 dispatch 未提供 Agent tool；以 r-implementer 對照 `project-conventions`
      / `coding-standards` skill 自審：collaborator 為 BaseBot 私有 composition、
      無新 IoC token、無反向 layer 邊；handler 端只接觸 collaborator 經 BaseBot
      暴露的 surface；無新 CJK 文字；contract 違規以 `TypeError` 表達；i18n key
      未新增。）
- [x] `docs/wiki/` 依 `update-wiki` skill 同步（C11 component page + CHANGELOG）

## R1 退場 acceptance

對齊 [progress.md → R1](progress.md#r1--拆解-basebot)。
