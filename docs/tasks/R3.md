# R3 任務 — plugins ↔ core/ioc 契約對齊

> 詳細設計：[docs/design/R3.md](../design/R3.md)　|　需求：[proposal §6](../proposal.md#6-r3--plugins--coreioc-契約對齊)　|　高階設計：[HLD §6](../high-level-design.md#6-r3--plugins--coreioc-契約對齊)

## 先決條件

- [ ] R2 已完成（`TOKENS.VoiceController` / `TOKENS.ModelCatalog` 已加入 tokens.ts，R3 才好一併經 barrel 暴露）

## 子任務

### 1. 擴張 core/plugin barrel

- [ ] `src/core/plugin/index.ts`：新增 re-export
  - `export { TOKENS } from '../ioc'`
  - `export type { ServiceToken, Resolver } from '../ioc'`
- [ ] 明示**不**re-export：`ServiceContainer`、`createContainer`、`token()`、container 的錯誤型別（保留只能由 composition root 使用）

### 2. 8 個 plugin 改 import 來源

- [ ] `src/plugins/auto-reply/plugin.ts`：`import { TOKENS } from '../../core/ioc'` → `'../../core/plugin'`
- [ ] `src/plugins/activity/plugin.ts`：同上
- [ ] `src/plugins/giveaway/plugin.ts`：同上
- [ ] `src/plugins/guild-events/plugin.ts`：同上
- [ ] `src/plugins/llm-chat/plugin.ts`：同上
- [ ] `src/plugins/message-backup/plugin.ts`：同上
- [ ] `src/plugins/voice/plugin.ts`：同上
- [ ] `src/plugins/earthquake/plugin.ts`：同上

### 3. ESLint 規則

- [ ] `eslint.config.mjs`：在既有 service-locator guard 之後新增 block：
  ```js
  {
    files: ['src/plugins/**/*.ts'],
    rules: {
      'no-restricted-imports': ['error', {
        patterns: [{
          group: ['**/core/ioc', '**/core/ioc/*', '@core/ioc', '@core/ioc/*'],
          message: 'Plugins must import TOKENS / ServiceToken from core/plugin, not core/ioc.',
        }],
      }],
    },
  }
  ```
- [ ] 確認與既有 service-locator block 不衝突

### 4. 規範文字寫入（4 份文件 verbatim）

- [ ] `CLAUDE.md`：插入 design R3 §7 規範段落（位置：plugin 規約相關章節）
- [ ] `CONTRIBUTING.md`：同段落（位置：建議的「新增 plugin」章節）
- [ ] `.claude/skills/project-conventions/SKILL.md`：同段落（位置：plugin 規約段落）
- [ ] `.claude/skills/coding-standards/SKILL.md`：同段落（位置：import / 邊界段落）
- [ ] 四份文件的段落文字逐字相同（便於日後一處改全處同步）

### 5. 驗證測試

- [ ] 新檔 `test/unit/core/plugin/barrel-exports.test.ts`：compile-time 驗證 `TOKENS / ServiceToken / Resolver` 從 `core/plugin` 可 import
- [ ] 新檔 `test/unit/eslint/plugin-ioc-import-rule.test.ts`（或加進既有 eslint config fixture 測試）：用 ESLint 程式化 API 對假 plugin 檔斷言違規

### 6. 驗證

- [ ] `grep -rln "from '.*core/ioc'" src/plugins` 結果為空
- [ ] `yarn lint` 全綠（包含新規則）
- [ ] `yarn typecheck` 全綠

## R3 退場 acceptance

對齊 [progress.md → R3](progress.md#r3--plugins--coreioc-契約對齊)。
