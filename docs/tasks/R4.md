# R4 任務 — 過長 handler 拆分 + 行數規範

> 詳細設計：[docs/design/R4.md](../design/R4.md)　|　需求：[proposal §7](../proposal.md#7-r4--過長-handler-拆分--規範)　|　高階設計：[HLD §7](../high-level-design.md#7-r4--過長-handler-拆分--規範)

## 先決條件

- [x] R3 已完成（共用 eslint 工序在同分支累積）

## 子任務

### 1. ESLint 規則新增

- [x] `eslint.config.mjs`：新增 `max-lines` block
  ```js
  {
    files: ['src/handlers/**/*.ts'],
    ignores: [
      'src/handlers/**/registry.generated.ts',
      // framework 檔，列為 follow-up
      'src/handlers/commands/command.ts',
      'src/handlers/commands/discord-helpers.ts',
      'src/handlers/reply-for-error.ts',
    ],
    rules: {
      'max-lines': ['error', { max: 150, skipBlankLines: false, skipComments: false }],
    },
  }
  ```
- [x] PR description 列出 ignores 中的 3 個 framework 檔作為 follow-up

### 2. 拆分 `db_list_message`（design R4 §5）

- [x] 新檔 `src/handlers/commands/db_list_message/parse-range.ts`：日期範圍解析（pure）
- [x] 新檔 `src/handlers/commands/db_list_message/render-reactions.ts`：reaction 文字渲染
- [x] 新檔 `src/handlers/commands/db_list_message/sanitize-mentions.ts`：mention 字串清洗
- [x] 新檔 `src/handlers/commands/db_list_message/format-message-lines.ts`：訊息行格式化
- [x] 新檔 `src/handlers/commands/db_list_message/chunk-output.ts`：輸出切塊
- [x] 新檔 `src/handlers/commands/db_list_message/build-archive-attachment.ts`：附件組裝
- [x] 重寫 `src/handlers/commands/db_list_message/index.ts`：≤ 120 行；只留 Discord I/O + 權限 + Translator + 回覆組裝
- [x] 對應 6 個 helper 各一支 unit test 於 `test/unit/handlers/db_list_message/`

### 3. 拆分 `inspect_member_ids`（design R4 §5）

- [x] 新檔 `src/handlers/commands/inspect_member_ids/parse-ids.ts`：ID 解析
- [x] 新檔 `src/handlers/commands/inspect_member_ids/format-helpers.ts`：通用格式化
- [x] 新檔 `src/handlers/commands/inspect_member_ids/format-member-fields.ts`：每位成員的描述欄位組裝
- [x] 重寫 `index.ts`：≤ 75 行
- [x] 對應 3 支 unit test

### 4. 拆分 `emoji_frequency`（design R4 §5）

- [x] 新檔 `src/handlers/commands/emoji_frequency/clamp-options.ts`：input 邊界 clamp
- [x] 新檔 `src/handlers/commands/emoji_frequency/aggregate-emoji-counts.ts`：聚合計數
- [x] 新檔 `src/handlers/commands/emoji_frequency/rank-emoji.ts`：排名
- [x] 新檔 `src/handlers/commands/emoji_frequency/format-leaderboard.ts`：榜單格式化
- [x] 重寫 `index.ts`：≤ 90 行
- [x] 對應 4 支 unit test

### 5. 拆分 `ai_settings`（design R4 §5）

- [x] 新檔 `src/handlers/commands/ai_settings/provider-choices.ts`：provider 選項表
- [x] 新檔 `src/handlers/commands/ai_settings/validate-ai-settings.ts`：input validation
- [x] 新檔 `src/handlers/commands/ai_settings/build-settings-modal.ts`：modal 組裝
- [x] 重寫 `index.ts`：≤ 60 行
- [x] 對應 3 支 unit test

### 6. 規範文字寫入（4 份文件 verbatim）

- [x] `CLAUDE.md`：插入 design R4 §7 的 5 點規範段落
- [x] `CONTRIBUTING.md`：同段落（位置：建議的「新增 handler」章節）
- [x] `.claude/skills/project-conventions/SKILL.md`：同段落
- [x] `.claude/skills/coding-standards/SKILL.md`：同段落
- [x] 四份文件段落逐字相同

### 7. 驗證

- [x] 4 個示範 handler 的 `index.ts` 全部 ≤ 150 行
- [x] 既有 handler 整合測試全綠（行為位元等價）
- [x] `yarn lint` 全綠（包含新 max-lines 規則）
- [x] `yarn handlers:gen:check` 全綠（codegen 不需重跑）

## R4 退場 acceptance

對齊 [progress.md → R4](progress.md#r4--handler-拆分--規範)。
