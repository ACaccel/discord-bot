# R5 任務 — i18n catalog 路徑反耦合

> 詳細設計：[docs/design/R5.md](../design/R5.md)　|　需求：[proposal §8](../proposal.md#8-r5--i18n-catalog-路徑反耦合)　|　高階設計：[HLD §8](../high-level-design.md#8-r5--i18n-catalog-路徑反耦合)

## 先決條件

- [x] R1 已完成（BaseBot 已重組；R5 的 `setupTranslator` 變更要在拆解後的結構上做）

## 子任務

### 1. catalog-loader 介面收窄

- [x] `src/core/i18n/catalog-loader.ts`：移除 `DEFAULT_LOCALES_DIR` 常數
- [x] `LoadCatalogOptions.localesDir` 改為**必填**（去掉 `?`）
- [x] `loadCatalogResources` 內部不再 fallback 到 default
- [x] `createDefaultTranslator` 簽名同步改為必須接 `localesDir`

### 2. BaseBot setupTranslator 重整

- [x] 在 BaseBot（R1 已重組後的版本）內新增私有 helper：`private resolveLocalesDir(): string` 或 `src/bot/locales-dir.ts` 獨立 helper（design R5 §3.3 建議放 BaseBot 內，理由：所有 4 個 bot 共用同一 monorepo 佈局）
- [x] `setupTranslator` 改為呼叫 `createDefaultTranslator({ localesDir: this.resolveLocalesDir() })`
- [x] 確認 4 個 bot subclass（Nijika/Konata/Tomori/MsgArchive）皆未 override `setupTranslator`；若無，subclass 不需動

### 3. 其他呼叫點同步

- [x] `src/deploy.ts`：2 個 `loadCatalogResources()` 呼叫點顯式傳入 `localesDir`
- [x] `test/i18n/catalog-runtime.test.ts`：4 個 `loadCatalogResources()` 呼叫點顯式傳入 `localesDir`
- [x] 既有 `test/unit/core/i18n/catalog-loader.test.ts` 已注入 `localesDir`，不需動（確認即可）

### 4. 新增整合 smoke 測試

- [x] 新檔（建議）`test/integration/i18n/bot-catalog-load.int.test.ts`：模擬 4 個 bot 各自啟動時 catalog 能正確 load

### 5. 驗證

- [x] `grep -n "'i18n'" src/core` 結果為空
- [x] `grep -rn 'DEFAULT_LOCALES_DIR' src` 結果為空
- [x] `yarn typecheck` 全綠（型別收窄後呼叫端遺漏會在編譯期被擋）
- [x] `yarn test` 全綠（catalog-completeness、catalog-loader、catalog-runtime 全部通過）

## R5 退場 acceptance

對齊 [progress.md → R5](progress.md#r5--i18n-catalog-路徑反耦合)。
