# R6 任務 — 低優先單點清理

> 詳細設計：[docs/design/R6.md](../design/R6.md)　|　需求：[proposal §9](../proposal.md#9-r6--低優先單點清理)　|　高階設計：[HLD §9](../high-level-design.md#9-r6--低優先單點清理)

## 先決條件與順序

- R6.4、R6.5：必須與 **R1 同分支同步**（R1 commit 之後立刻做）
- R6.1、R6.2：R1 之後任意時點
- R6.3：建議放最後，避免被 R6.1 / R6.2 引入的程式碼改動再次觸碰

---

## R6.1 — traceId crypto.randomUUID

- [x] `src/bot/index.ts`（router middleware 段落）：
  - [x] `import { randomUUID } from 'node:crypto'`
  - [x] 將 `Math.random().toString(36).slice(2, 10)` 改為 `randomUUID().slice(0, 8)`
- [x] 對應單元測試（或整合測試 spy）：
  - [x] traceId 長度 = 8
  - [x] traceId 僅含 hex 字元（0-9a-f）
  - [x] 1000 次生成無碰撞（機率性，效率夠）

---

## R6.2 — login() 失敗 reject

- [x] `src/bot/index.ts` 的 `login()`：
  - [x] 改為 `await this.client.login(this.token)` 並在 catch 拋 `ConfigurationError`
  - [x] code = `BOT_LOGIN_FAILED`、messageKey = `errors:bot.login_failed`
  - [x] 對 `!this.client.user` guard 也丟對應錯誤（code = `BOT_LOGIN_NO_USER`、messageKey = `errors:bot.login_no_user`）
- [x] i18n key 新增：
  - [x] `src/i18n/locales/zh-TW/errors.json`：`bot.login_failed`、`bot.login_no_user`
  - [x] `src/i18n/locales/en/errors.json`：對應同 2 key
- [x] 測試：
  - [x] mock `client.login` reject → `BaseBot.run()` promise reject 為 `ConfigurationError`，且 `startAll()` 沒被觸發

---

## R6.3 — console.\* 清除 + ESLint 提 error

- [x] 用 design R6 §5 的盤點表逐檔處理 23 處 `console.*`：
  - [x] 類別 (a) 一般 logger 替換：改用 `bot.logger.info / warn / error`
  - [x] 類別 (b) 啟動前 phase：使用 `createBootstrapLogger()`
  - [x] 類別 (c) last-resort（保留）：加 `// last-resort` 註解，並在 ESLint allow 內保留
  - [x] 類別 (d) 已 commented-out 的死碼：刪除
- [x] `eslint.config.mjs`：
  - [x] `no-console` 由 `['warn', { allow: ['warn', 'error'] }]` 改為 `['error', { allow: ['error'] }]`
- [x] 驗證：`grep -rn 'console\.' src --include='*.ts'` 結果為空，或只剩有 `// last-resort` 註解的 `console.error`
- [x] `yarn lint` 全綠

---

## R6.4 — BaseBot 命名一致化

- [x] `src/bot/index.ts` 欄位改名（在 R1 重組時直接套用最終命名；若 R1 已套用，此處只需檢查）：
  - [x] `buttonHandler` → `buttonHandlers`
  - [x] `ssmHandler` → `ssmHandlers`
  - [x] `modalHandler` → `modalHandlers`
  - [x] `reactionHandler` → `reactionHandlers`
  - [x] `help_msg` → `helpMessage`
  - [x] `guild_num` → `guildCount`（若為類別欄位）
  - [x] `debug_ch` → `debugChannel`（若為類別欄位）
- [x] 下游呼叫點同步（grep 結果）：
  - [x] `src/handlers/buttons/**/*.ts`
  - [x] `src/handlers/select-menus/**/*.ts`
  - [x] `src/handlers/modals/**/*.ts`
  - [x] `src/handlers/reactions/**/*.ts`
  - [x] `src/handlers/commands/help/index.ts`
  - [x] `src/bot/nijika/nijika.ts`（comment）
  - [x] 其他編譯期錯誤點全部修正
- [x] `yarn typecheck` 全綠（型別擋下遺漏）
- [x] CLAUDE.md「命名」段落更新（若有舉例舊命名）

---

## R6.5 — `src/bot/index.ts` import 排序

- [x] 在 R1 重組時順手套用：
  - [x] 所有 `import` 移到檔案最上方連續區塊
  - [x] `sharedConnectionManagers` map 與 helper 函式搬到 import 區段之後
- [x] `eslint.config.mjs`：新增 `'import/first': 'error'`（`eslint-plugin-import` 已在 devDependencies，無需新增依賴）
- [x] `yarn lint` 全綠

---

## R6 整體驗證

- [x] 全部 5 子項完成
- [x] `yarn typecheck && yarn lint && yarn test && yarn format:check` 全綠
- [x] `reliability-reviewer` agent Audit R6.2 變更，verdict = PASS

## R6 退場 acceptance

對齊 [progress.md → R6](progress.md#r6--低優先清理)。
