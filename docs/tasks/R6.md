# R6 任務 — 低優先單點清理

> 詳細設計：[docs/design/R6.md](../design/R6.md)　|　需求：[proposal §9](../proposal.md#9-r6--低優先單點清理)　|　高階設計：[HLD §9](../high-level-design.md#9-r6--低優先單點清理)

## 先決條件與順序

- R6.4、R6.5：必須與 **R1 同分支同步**（R1 commit 之後立刻做）
- R6.1、R6.2：R1 之後任意時點
- R6.3：建議放最後，避免被 R6.1 / R6.2 引入的程式碼改動再次觸碰

---

## R6.1 — traceId crypto.randomUUID

- [ ] `src/bot/index.ts`（router middleware 段落）：
  - [ ] `import { randomUUID } from 'node:crypto'`
  - [ ] 將 `Math.random().toString(36).slice(2, 10)` 改為 `randomUUID().slice(0, 8)`
- [ ] 對應單元測試（或整合測試 spy）：
  - [ ] traceId 長度 = 8
  - [ ] traceId 僅含 hex 字元（0-9a-f）
  - [ ] 1000 次生成無碰撞（機率性，效率夠）

---

## R6.2 — login() 失敗 reject

- [ ] `src/bot/index.ts` 的 `login()`：
  - [ ] 改為 `await this.client.login(this.token)` 並在 catch 拋 `ConfigurationError`
  - [ ] code = `BOT_LOGIN_FAILED`、messageKey = `errors:bot.login_failed`
  - [ ] 對 `!this.client.user` guard 也丟對應錯誤（code = `BOT_LOGIN_NO_USER`、messageKey = `errors:bot.login_no_user`）
- [ ] i18n key 新增：
  - [ ] `src/i18n/locales/zh-TW/errors.json`：`bot.login_failed`、`bot.login_no_user`
  - [ ] `src/i18n/locales/en/errors.json`：對應同 2 key
- [ ] 測試：
  - [ ] mock `client.login` reject → `BaseBot.run()` promise reject 為 `ConfigurationError`，且 `startAll()` 沒被觸發

---

## R6.3 — console.\* 清除 + ESLint 提 error

- [ ] 用 design R6 §5 的盤點表逐檔處理 23 處 `console.*`：
  - [ ] 類別 (a) 一般 logger 替換：改用 `bot.logger.info / warn / error`
  - [ ] 類別 (b) 啟動前 phase：使用 `createBootstrapLogger()`
  - [ ] 類別 (c) last-resort（保留）：加 `// last-resort` 註解，並在 ESLint allow 內保留
  - [ ] 類別 (d) 已 commented-out 的死碼：刪除
- [ ] `eslint.config.mjs`：
  - [ ] `no-console` 由 `['warn', { allow: ['warn', 'error'] }]` 改為 `['error', { allow: ['error'] }]`
- [ ] 驗證：`grep -rn 'console\.' src --include='*.ts'` 結果為空，或只剩有 `// last-resort` 註解的 `console.error`
- [ ] `yarn lint` 全綠

---

## R6.4 — BaseBot 命名一致化

- [ ] `src/bot/index.ts` 欄位改名（在 R1 重組時直接套用最終命名；若 R1 已套用，此處只需檢查）：
  - [ ] `buttonHandler` → `buttonHandlers`
  - [ ] `ssmHandler` → `ssmHandlers`
  - [ ] `modalHandler` → `modalHandlers`
  - [ ] `reactionHandler` → `reactionHandlers`
  - [ ] `help_msg` → `helpMessage`
  - [ ] `guild_num` → `guildCount`（若為類別欄位）
  - [ ] `debug_ch` → `debugChannel`（若為類別欄位）
- [ ] 下游呼叫點同步（grep 結果）：
  - [ ] `src/handlers/buttons/**/*.ts`
  - [ ] `src/handlers/select-menus/**/*.ts`
  - [ ] `src/handlers/modals/**/*.ts`
  - [ ] `src/handlers/reactions/**/*.ts`
  - [ ] `src/handlers/commands/help/index.ts`
  - [ ] `src/bot/nijika/nijika.ts`（comment）
  - [ ] 其他編譯期錯誤點全部修正
- [ ] `yarn typecheck` 全綠（型別擋下遺漏）
- [ ] CLAUDE.md「命名」段落更新（若有舉例舊命名）

---

## R6.5 — `src/bot/index.ts` import 排序

- [ ] 在 R1 重組時順手套用：
  - [ ] 所有 `import` 移到檔案最上方連續區塊
  - [ ] `sharedConnectionManagers` map 與 helper 函式搬到 import 區段之後
- [ ] `eslint.config.mjs`：新增 `'import/first': 'error'`（`eslint-plugin-import` 已在 devDependencies，無需新增依賴）
- [ ] `yarn lint` 全綠

---

## R6 整體驗證

- [ ] 全部 5 子項完成
- [ ] `yarn typecheck && yarn lint && yarn test && yarn format:check` 全綠
- [ ] `reliability-reviewer` agent Audit R6.2 變更，verdict = PASS

## R6 退場 acceptance

對齊 [progress.md → R6](progress.md#r6--低優先清理)。
