/**
 * @deprecated Re-export shim — see `src/db/schema.ts` for rationale.
 *   Doc types are owned by `src/persistence/schemas/`.
 */
export type {
  FetchDoc,
  MessageDoc,
  ReplyDoc,
  TodoDoc,
  GiveawayDoc,
  ActivityDoc,
  UserApiSettingDoc,
  DocByName,
} from '../persistence/schemas';
