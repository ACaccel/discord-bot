{
  "compilerOptions": {
    "target": "ES2021",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "rootDir": ".",
    "baseUrl": ".",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,

    "noEmit": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "useUnknownInCatchVariables": true,

    "skipDefaultLibCheck": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  // Phase 0: strict typechecking applies only to newly-added code paths.
  // Each later phase that converts a legacy directory adds its path here.
  // Phase 1 expansion: `src/db/**` — schemas now have typed doc interfaces
  // (no more `Record<string, Model<any>>`) and the connection factory
  // returns a strongly-typed `Models` map.
  "include": ["src/core/**/*", "src/db/**/*", "scripts/**/*", "test/**/*"],
  "exclude": ["node_modules", "dist"]
}
